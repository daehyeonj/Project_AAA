# Skin Assignment Template

Fill this manifest before any final sprite assignment if the team wants a written slot plan first.

Codex may only assign sprites after the user provides an explicit mapping.

## Battle

- `PanelBackground`:
- `CommandButtonNormal`:
- `CommandButtonHover`:
- `CommandButtonSelected`:
- `PopupBackground`:

## Inventory

- `EquipmentSlotEmpty`:
- `EquipmentSlotEquipped`:
- `EquipmentSlotSelected`:
- `ItemRowNormal`:
- `ItemRowSelected`:
- `RunSpoilsBadge`:
- `PendingExtractionBadge`:
