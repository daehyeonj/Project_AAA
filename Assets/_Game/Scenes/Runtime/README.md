# Runtime Scenes

This folder is for runtime-facing scenes that are safe to keep on the active app rail.

Current contents:

- `Boot.Unity`

Current legacy exception:

- `Assets/Scenes/SampleScene.unity` remains the accepted playable baseline until a dedicated scene migration batch updates Build Settings, editor defaults, smoke tooling, and docs together.
